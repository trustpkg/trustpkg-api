root = "."
tmp_dir = "tmp"

[build]
  cmd = "templ generate; go build -o ./bin/replicator-service.exe ./cmd/replicator-service/main.go"
  entrypoint = "./bin/replicator-service.exe"

  include_ext = ["go"]
  include_dir = ["cmd", "internal"]

  exclude_dir = [
    "tmp",
    "vendor",
    "bin",
  ]

  delay = 1000
  stop_on_error = true